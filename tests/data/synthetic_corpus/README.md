# Synthetic Corpus

Store deterministic synthetic documents for repeatable tests.

Goals:

- Controlled entity relationships for graph traversal assertions.
- Known ground truth answers for retrieval and generation checks.
- Small and fast datasets for CI.
